ПЛАН 2: ГЛОБАЛЬНЫЙ АУДИТ И РЕФАКТОРИНГ
========================================

1. ОПТИМИЗАЦИЯ СЕТИ (XHR/FETCH):
   - Аудит всех API-вызовов в index.html
   - Перевод оставшихся fetch() на XHR через rvXhr() / единый механизм
   - Добавить обработку таймаутов для всех запросов
   - Особое внимание: чат поддержки, загрузка логов, авторизация

2. БЕЗОПАСНОСТЬ БЭКЕНДА:
   - Аудит freeapi/routes.py: проверка session_required на всех эндпоинтах
   - Валидация MIME-типов для загрузки файлов (.session и картинок)
   - Ограничение размера файлов на сервере
   - Защита от переполнения памяти в Termux

3. UI/UX ПОЛИРОВКА (ЧБ СТИЛЬ):
   - Убрать синие focus-outline (заменить на серые/прозрачные)
   - Проверить контрастность серого текста на чёрном фоне
   - Плавные анимации для модалок и дропдаунов
   - Мобильная адаптивность: таблицы, длинные сообщения в чате

4. СИСТЕМА УВЕДОМЛЕНИЙ (TOASTS):
   - Проверить стекинг тостов
   - Корректное исчезновение и z-index

ШАГИ:
  - Аудит routes.py -> фикс безопасности -> git push
  - Аудит CSS -> фикс outline/контраст -> git push
  - Аудит fetch -> замена на XHR -> git push
  - Тосты -> фикс -> финальный git push

СТАТУС: ОЖИДАЕТ ЗАВЕРШЕНИЯ ЭТАПА 1
