═══════════════════════════════════════════════════════════════════════
PLAN — Сессия глубокой разработки (продолжение)
═══════════════════════════════════════════════════════════════════════
Дата: 2026-04-26
Окружение: Termux-проект (Flask + vanilla JS), репо FreeApi-Python.

ЗАДАЧИ ПОЛЬЗОВАТЕЛЯ
───────────────────────────────────────────────────────────────────────
[1] При отвязке Telegram слать в личку TG сообщение
    «Уведомления отвязаны от аккаунта <ник_сайта>».
[2] Окно настройки (Setup-модалка) не обновляется в реал-тайме —
    бот уже на 3-минутной фазе обучения, а UI статичен.
[3] При нажатии на крестик у Setup-модалки во время активной
    настройки — спрашивать «Прервать или продолжить в фоне?».
    Если «Продолжить» — спрятать модалку, на дашборде показать
    мини-виджет с ником/номером аккаунта, анимированным
    «Настройка…» и текущим этапом. Клик по виджету — снова
    открыть полноценную модалку с прогрессом.
    Бонус: после завершения фоновой настройки ключ должен
    автоматически появиться в дашборде.
[4] (новое) Уведомление в личку TG о завершении/ошибке
    фоновой настройки — чтобы юзер не залипал на дашборде.

ШАГИ ВЫПОЛНЕНИЯ
───────────────────────────────────────────────────────────────────────
[X] S0  Клонировать репозиторий и провести аудит проекта.
[X] S1  Создать и закоммитить plan.txt в репо + workspace.
[X] S2  Task 1: добавить ник сайта в прощальное сообщение TG-бота
        в freeapi/blueprints/community_bp.py (community_tg_link_unlink).
[X] S3  Task 2: исправить интервал polling-фолбека в
        static/js/views/setup.js (3600000 ms → 3000 ms).
[X] S4  Task 3.1 (бэк): добавить endpoint GET /api/tg/setup/running.
[X] S5  Task 3.2 (HTML): confirm-модалка #closeSetupConfirmModal +
        контейнер мини-виджета #setupMiniCard.
[X] S6  Task 3.3 (CSS): стили мини-виджета (анимированный спиннер,
        прогресс-бар, состояния done/error, адаптив).
[X] S7  Task 3.4 (JS): логика закрытия Setup-модалки + рендер
        мини-виджета + реалтайм + reopen.
[X] S8  Task 3.5: подхват running setup на загрузке дашборда (F5).
[X] S9  Push #1 (commit 858f399).
[X] S10 Task 4: личное TG-уведомление о результате фоновой настройки
        (success → ник сайта + tg-аккаунт + сам API-ключ;
        error → ник сайта + tg-аккаунт + причина + подсказка
        нажать «Повторить»). Хук добавлен в SetupRunner.run()
        в freeapi/tg.py — best-effort, не валит основной поток.
[X] S11 Push #2 (текущий коммит).

СТАТУС: ВСЁ ВЫПОЛНЕНО.
