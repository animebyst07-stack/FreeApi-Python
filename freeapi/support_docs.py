# Реестр документации для агента поддержки FavoriteAPI.
# Идея: вместо того чтобы каждый раз грузить в ИИ ~300 КБ исходников,
# системный промпт даёт ИИ компактный список тегов (DOCS_INDEX), а ИИ
# по запросу пользователя сам решает, какую справку «прочитать», написав
# в ответе тег вида [[doc:NAME]]. Сервер ловит тег, подменяет его текстом
# из DOCS и просит ИИ продолжить ответ.
#
# Правила добавления нового раздела:
#   1) Короткое имя в snake_case (только латиница / цифры / _).
#   2) Краткое описание в DOCS_INDEX (одна строка, для ИИ).
#   3) Полный текст справки в DOCS (1-3 КБ, по делу, без воды).

DOCS = {
    'menu': """=== ГЛАВНОЕ МЕНЮ И НАВИГАЦИЯ ===
На сайте слева выезжает sidebar (кнопка «гамбургер» сверху). Разделы:
  • Дашборд — список API-ключей пользователя, общая статистика.
  • Чат — встроенный AI-чат для проверки ключа без внешнего клиента.
  • Документация — описание REST API (OpenAI-совместимое) и примеры.
  • Отзывы — общий список + форма «Оставить отзыв».
  • Поддержка — этот самый чат с тобой (Favorite AI Agent).
  • Уведомления (колокольчик в шапке) — личные оповещения пользователя.
  • Администрирование — виден только админу (ReZero).

Шапка: логотип FavoriteAPI слева, колокольчик уведомлений и гамбургер справа.
""",

    'auth': """=== АВТОРИЗАЦИЯ И УЧЁТНАЯ ЗАПИСЬ ===
  • Регистрация: логин (3-32 символа, латиница/цифры/_) + пароль (мин. 6 символов).
    Один логин = один аккаунт. Email не требуется.
  • Логин: тот же логин и пароль. Сессия в cookie, держится долго.
  • Выход: пункт «Выйти» в шапке/sidebar.
  • Восстановление пароля через сайт пока не реализовано — в этом случае
    предложи пользователю завершить диалог, чтобы запрос дошёл до админа.
  • Имя пользователя сохраняется в session['username'] и видно админу в отзывах
    и в админских уведомлениях.
""",

    'api_keys': """=== API-КЛЮЧИ ===
Что такое: ключ FavoriteAPI = OpenAI-совместимый Bearer-токен, через который
любой клиент (curl, OpenAI SDK, приложения) может ходить в наш REST API
по адресу /v1/chat/completions. Каждый ключ привязан к ОДНОМУ Telegram-аккаунту
пользователя (через Telethon-сессию) и к модели по умолчанию.

Действия в Дашборде:
  • Создать ключ — мастер из 2 шагов: загрузка .session-файла (или вход по
    номеру + код), затем выбор модели по умолчанию. После создания показывается
    «сырой» ключ ОДИН РАЗ — его надо скопировать сразу.
  • Шестерёнка у ключа — настройки: переименование, смена модели по умолчанию,
    включение dual-mode (двух аккаунтов: основной + переводчик).
  • Удалить ключ — мягкое удаление (is_active=0). Восстановление через
    интерфейс не предусмотрено: «pending_restore» относится к памяти агента
    (context_md/favorite_md) после /reset, а не к API-ключам.
  • Иконка истории — список последних запросов через ключ.

Лимиты и состояния:
  • is_busy=1 — ключ сейчас занят запросом, новый вернёт KEY_BUSY_301.
  • CTX_LIMIT_180 — переполнен контекст у Telegram-бота, нужен /reset
    (см. doc:errors и doc:chat).
  • context_kb / context_tokens — текущий накопленный контекст, лимит ~CONTEXT_LIMIT_KB.

Интроспекция ключа из внешнего клиента (без захода в Дашборд):
  • GET /api/v1/me (Bearer) — один запрос отдаёт всё, что нужно
    внешнему клиенту/ИИ: имя ключа (масированное), default_model,
    dual_mode, текущий context_kb, лимиты (warn/limit), is_busy,
    статистику за месяц и владельца. Рекомендуется вызывать один
    раз в начале сессии — это закрывает кейсы «не знаю какая
    модель default» и «не знаю, насколько забит контекст».
  • GET /api/v1/models (Bearer) — каноничный список моделей с
    флагами supportsVision / contextK / isRecommended и явным
    keyDefaultModelId.
""",

    'tg_setup': """=== ПОДКЛЮЧЕНИЕ TELEGRAM-АККАУНТА ===
FavoriteAPI работает как прокси к Telegram-боту «Сэм – ChatGPT нейросеть»
(@chatgpt_botbot и аналоги): мы шлём твоё сообщение в личку боту от имени
твоего Telegram-аккаунта и забираем ответ. Поэтому каждому ключу нужен
живой Telegram-аккаунт, к которому у нас есть Telethon-сессия.

Два способа подключения (вкладки в мастере создания ключа):
  1) Импорт .session-файла (Telethon StringSession в base64 или .session SQLite).
     Файл загружается через POST /api/tg/session/import.
  2) По номеру телефона: API_ID + API_HASH с my.telegram.org → сервер шлёт
     запрос кода → пользователь вводит код из Telegram (5 цифр), при 2FA
     ещё пароль облака. Прогресс смотри через /api/tg/setup/<id>/status.

После успешной авторизации Telethon сохраняет StringSession, мы шифруем её
(см. encrypt_text в security.py) и пишем в tg_accounts.session_b64.
Поле setup_done=1 означает «аккаунт готов к работе».

Если код не приходит/неверный — попроси перезапросить, проверить, что
номер в международном формате (+71234567890), и убедиться, что аккаунт
не заблокирован Telegram.
""",

    'models': """=== МОДЕЛИ ИИ ===
Список моделей задан в freeapi/models.py (AI_MODELS) и зеркалит то, что
поддерживает Telegram-бот «Сэм»: Gemini 1.5/2.5/3.0 (Flash, Pro, thinking,
Robotics), GPT-4o/4.1/5, Claude и пр. DEFAULT_MODEL_ID — модель по умолчанию.

Где меняется модель:
  • При создании ключа — выбор default_model.
  • Через шестерёнку ключа в Дашборде — смена default_model.
  • В REST-запросе можно явно указать "model": "<id>" — тогда сервер на лету
    переключит модель у бота (switch_model_background) перед ответом.
  • В чате на сайте — выпадающий список моделей.

Если модель «не отвечает», обычно дело в том, что бот ушёл на тяжёлую модель
(thinking) и просто долго думает (до 90 секунд). Если ответа нет совсем —
проверяй ключ в Дашборде (is_busy, последние ошибки).

Программный список моделей:
  • GET /api/models — публичный, со статистикой использования.
  • GET /api/v1/models (Bearer) — каноничный, под текущий ключ:
    {models[id, displayName, contextK, supportsVision, isDefault,
    isPopular, isRecommended], defaultModelId, keyDefaultModelId,
    recommended}. keyDefaultModelId — модель, которая будет
    использована, если в /api/v1/chat не указать поле "model".
""",

    'chat': """=== ВСТРОЕННЫЙ ЧАТ И КОНТЕКСТ ===
Раздел «Чат» — это упрощённый клиент для проверки ключа без внешнего
приложения. Под капотом тот же /v1/chat/completions.

Контекст:
  • Бот «Сэм» хранит историю диалога на своей стороне (привязано к
    Telegram-аккаунту ключа).
  • Накопленный объём отслеживается в api_keys.context_kb / context_tokens.
  • При CONTEXT_WARN_KB UI подсказывает «контекст растёт», при CONTEXT_LIMIT_KB —
    бот вернёт CTX_LIMIT_180 и нужен сброс.
  • Команда /reset (run_control в freeapi/tg.py) полностью очищает память
    у бота. На стороне сервера тоже есть clear_context(key_id) в memory.py.
  • Теги управления контекстом: команды парсятся в memory.parse_tags /
    process_commands (например автоматическая подкачка фактов).
""",

    'reviews': """=== ОТЗЫВЫ ===
Раздел «Отзывы» — общедоступная лента + форма «Оставить отзыв» (видна
авторизованным). Один пользователь = один отзыв (UNIQUE INDEX
idx_reviews_one_per_user). Чтобы переписать отзыв — сначала удалить старый
кнопкой «Удалить» в карточке.

Поля отзыва: оценка от 1 до 10 (числовая шкала, без звёзд), текст,
опционально до нескольких изображений.
После публикации отзыв уходит на модерацию (status='pending'). Админ может:
  • Одобрить (status='approved') — отзыв виден всем.
  • Отклонить (status='rejected') — виден только автору с пометкой.
  • Ответить лично или сгенерировать AI-ответ (reply_by='ai').

Ограничения: review_restrictions.banned_until — бан на оставление отзывов
до указанной даты (накладывает админ при злоупотреблении).
""",

    'support': """=== ПОДДЕРЖКА (этот чат) ===
Ты — Favorite AI Agent, AI-ассистент в разделе «Поддержка». Архитектура:
  • POST /api/support/chat — пользователь шлёт сообщение, сервер сохраняет
    его в support_messages (role='user'), формирует запрос к Сэму через
    тот же ключ что и обычные чаты (support_key_id в admin_settings).
  • На ПЕРВОМ сообщении нового диалога сервер делает /reset Сэму и шлёт
    полный системный промпт + индекс документации (тебя). На последующих —
    только новый текст пользователя (Сэм держит историю).
  • Ты можешь запросить документацию тегом [[doc:NAME]] (см. doc:menu для
    списка разделов сайта; список доступных тегов в системном промпте).
  • Кнопка «Завершить» — POST /api/support/close: ты получишь весь диалог
    и должен решить, нужно ли создать notification админу (формат JSON
    {needs_report, report_text, summary}). После закрытия Сэм получает /reset.
  • История диалогов хранится в support_chats / support_messages, видна
    только автору и админу.
""",

    'admin': """=== АДМИНИСТРИРОВАНИЕ ===
Раздел «Администрирование» виден только пользователю с ролью админа (ReZero).
Возможности:
  • Управление настройками (admin_settings: ключ агента, ключ поддержки,
    модель поддержки, кастомный системный промпт поддержки).
  • Лента уведомлений (отзывы на модерации, отчёты из закрытых чатов
    поддержки, прочие алерты).
  • Управление отзывами (одобрить/отклонить/ответить/удалить, баны).
  • Просмотр логов (ring-buffer в памяти + дамп на Ctrl+C/SIGTERM).

Если пользователь жалуется «не вижу админку» — он не админ, это нормально.
""",

    'errors': """=== ТИПИЧНЫЕ КОДЫ ОШИБОК ===
  • KEY_INVALID_302 — Authorization: Bearer не передан или ключ
    деактивирован/удалён. Решение: проверить ключ в Дашборде.
  • KEY_BUSY_301 — ключ занят другим запросом. Параллельные запросы через
    один ключ запрещены. Решение: подождать или создать второй ключ.
  • KEY_NO_TG_303 — у ключа нет рабочего Telegram-аккаунта (setup_done=0
    или аккаунт удалён). Решение: пройти мастер подключения заново.
  • CTX_LIMIT_180 — у Сэма переполнен контекст. Сервер пытается
    автоматически сделать /reset и повторить (см. doc:chat).
  • Таймауты на тяжёлых моделях (thinking) — норма до ~90 секунд.
  • Ошибка отправки кода Telegram при настройке — обычно неверный формат
    номера или временная блокировка от Telegram.
""",

    'stats': """=== СТАТИСТИКА ===
  • /api/stats/global — общие цифры по сервису (всего ключей, запросов,
    активных пользователей). Видна на дашборде.
  • На карточке ключа в Дашборде показывается: количество запросов,
    последняя активность, текущий контекст в КБ.
  • История запросов конкретного ключа открывается из карточки (иконка часов):
    модель, время, размер ответа, статус.
""",

    'notifications': """=== УВЕДОМЛЕНИЯ ===
  • Личные уведомления пользователя — колокольчик в шапке. Бэйдж показывает
    число непрочитанных. Опрос /api/notifications каждые 60 секунд.
  • Источники уведомлений: решения AI-модератора по твоему отзыву
    (APPROVE / FEEDBACK / DELETE с причиной и сроком ограничения), ручной
    ответ владельца на отзыв, системные сообщения. Алертов по ключам пока нет.
  • Уведомления сгруппированы по типам: «Отзывы», «Поддержка», «Система» —
    переключение вкладками над списком (B2).
  • Кнопка «Прочитать всё» / удаление по одному в выпадающей панели.
  • Админские уведомления — отдельная лента в разделе «Администрирование»
    (новые отзывы, отчёты из закрытых диалогов поддержки).
""",
}


# Краткий индекс для системного промпта: имя → одна строка описания.
# Должен помещаться в пару сотен байт, чтобы не раздувать промпт.
DOCS_INDEX_LINES = {
    'menu':          'главное меню сайта, разделы, навигация',
    'auth':          'регистрация, логин, выход, восстановление пароля',
    'api_keys':      'API-ключи: создание, настройка, удаление, лимиты',
    'tg_setup':      'подключение Telegram-аккаунта (.session или код)',
    'models':        'список моделей ИИ и переключение',
    'chat':          'встроенный чат, контекст, /reset, теги памяти',
    'reviews':       'отзывы: написание, модерация, лимиты',
    'support':       'как устроена поддержка (этот чат) изнутри',
    'admin':         'возможности админа',
    'errors':        'коды ошибок: KEY_BUSY_301, KEY_INVALID_302 и пр.',
    'stats':         'где смотреть статистику использования',
    'notifications': 'уведомления (колокольчик, админская лента)',
}


def docs_index_text():
    """Компактный список доступных тегов для подстановки в системный промпт."""
    lines = ['=== ДОСТУПНАЯ ДОКУМЕНТАЦИЯ (используй теги [[doc:NAME]]) ===']
    for name, desc in DOCS_INDEX_LINES.items():
        lines.append(f'  • [[doc:{name}]] — {desc}')
    return '\n'.join(lines)


def get_doc(name):
    """Вернёт текст справки по имени или None, если такого тега нет."""
    return DOCS.get((name or '').strip().lower())
