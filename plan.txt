════════════════════════════════════════════════════════════════════
  PLAN.TXT — FreeApi-Python
  Полностью перечитан и актуализирован: 23 апреля 2026
  Основан на детальном прочтении ВСЕХ файлов проекта.
════════════════════════════════════════════════════════════════════

──────────────────────────────────────────────────────────
АКТУАЛЬНЫЙ СТЕК И СТРУКТУРА
──────────────────────────────────────────────────────────

  Python 3.11 + Flask 3 + SQLite + Telethon
  Фронт: SPA, static/index.html + static/js/* + static/css/main.css
  Запуск: python api.py (Termux, порт 5000)
  Репо: github.com/animebyst07-stack/FreeApi-Python
  Git push: только через GitHub API (токен в scratchpad)

  Ключевые директории:
    freeapi/
      app.py              — Flask-приложение, регистрация blueprint'ов
      agent.py            — FavoriteAIAgent (AI-модератор отзывов, фоновый поток)
      database.py         — SQLite, система миграций schema_migrations
      models.py           — AI_MODELS (11 моделей), DEFAULT_MODEL_ID
      support_docs.py     — реестр документации для агента поддержки
      repos/              — репо-функции (keys, stats, notifications, reviews, ...)
      blueprints/         — Flask blueprint'ы (chat, reviews, notifications, admin, support, ...)
      migrations/         — 001..006 SQL-миграции (006 = review_likes)
    static/
      index.html          — главный SPA (большой объём inline JS/CSS)
      css/main.css        — весь CSS
      js/
        core/             — api.js, dom.js, logger.js, storage.js, state.js
        ui/               — modal.js, select.js, sidebar.js, toast.js, topbar.js, view.js
        views/            — keys.js, notifications.js, support.js, auth.js, ...
                            ВНИМАНИЕ: reviews.js — только заглушка (export {}),
                            реальный код отзывов пока в inline-скрипте index.html.

  Модели: 11 моделей Gemini 1.5/2.5/3.0 (Flash, Pro, Thinking, Robotics).
  Оценка отзывов: 1–10 (НЕ 1–5, несмотря на устаревшие комментарии в docs).
  Админ: username = 'ReZero' (хардкод во всех проверках).

──────────────────────────────────────────────────────────
ВЫПОЛНЕНО ДО ЭТОЙ СЕССИИ
──────────────────────────────────────────────────────────

  ✓ Этап 0 (0.1–0.6) — рефакторинг JS/CSS/бэкенда закрыт.
  ✓ 0.6 — честная система миграций schema_migrations в database.py.
  ✓ 0.5.12 — уведомления вынесены в views/notifications.js.
  ✓ 0.5.13 — поддержка вынесена в views/support.js.
  ✓ reviews_bp.py — лайки, score 1-10, avg_score, фото, модератор.
  ✓ agent.py — FavoriteAIAgent, AI-модерация pending-отзывов.
  ✓ support_bp.py — SSE-стрим, doc-теги [[doc:NAME]], SUPPORT_CLOSE_PROMPT.
  ✓ support_docs.py — реестр документации для поддержки.
  ✓ localStorage autosave черновика отзыва (уже есть в index.html):
      oninput → lsSet('freeapi_review_draft', value)
      после отправки → lsDel('freeapi_review_draft')
      при открытии формы → lsGet('freeapi_review_draft', '')
      НО: баннер «Найден черновик [Восстановить] [Удалить]» ещё не сделан.

──────────────────────────────────────────────────────────
ЗАДАЧИ ЭТОЙ СЕССИИ
──────────────────────────────────────────────────────────

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
БЛОК A — ФИКСЫ (баги + UI)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

────────────────────────────────────────
A0 — Эмодзи в уведомлениях (нарушение правила)
────────────────────────────────────────
Статус: [✓] ВЫПОЛНЕНО
Сложность: минимальная (1 файл, 4 строки).

Проблема:
  В freeapi/blueprints/reviews_bp.py строки 225, 227, 229, 231 содержат
  символ 👑 в текстах уведомлений — нарушение правила «эмодзи запрещены».
  Тексты попадают в user_notifications.message и отображаются в /notifications.

Текущий код:
  msg = f'👑 Владелец ответил на ваш отзыв (с фото): {snippet}'
  msg = f'👑 Владелец ответил на ваш отзыв: {snippet}'
  msg = '👑 Владелец прикрепил фото к ответу на ваш отзыв.'
  msg = '👑 Владелец отметил ваш отзыв.'

Решение:
  Заменить 👑 на «[Владелец]» (или убрать вовсе — без обозначения):
  msg = f'Владелец ответил на ваш отзыв (с фото): {snippet}'
  msg = f'Владелец ответил на ваш отзыв: {snippet}'
  msg = 'Владелец прикрепил фото к ответу на ваш отзыв.'
  msg = 'Владелец отметил ваш отзыв.'

Push: «fix(reviews): A0 — убраны эмодзи из текстов уведомлений».

────────────────────────────────────────
A1 — Отступ сверху: «Новый ключ» и «Назад к дашборду»
────────────────────────────────────────
Статус: [✓] ВЫПОЛНЕНО
Сложность: минимальная (1 CSS-файл, 2 строки).

Проблема:
  Кнопка «+ Новый ключ» / заголовок «API КЛЮЧИ» прилипают к шапке.
  Кнопка «Назад к дашборду» тоже вплотную к шапке.
  Шапка занимает 53px (class .topbar height). .dash{padding-top:36px}
  на практике ощущается как слишком малый отступ.

Файл: static/css/main.css
  Строка 191: .dash{padding:36px 0 64px;position:relative;z-index:1}
  Строка 192: @media (max-width:640px){.dash{padding-top:24px}}

Решение:
  .dash { padding: 52px 0 64px; position: relative; z-index: 1 }
  @media (max-width:640px) { .dash { padding-top: 40px } }

Push: «fix(ui): A1 — увеличен padding-top для .dash».

────────────────────────────────────────
A2 — Статистика моделей показывает 0 req
────────────────────────────────────────
Статус: [✓] ВЫПОЛНЕНО
Сложность: малая (1 файл, ~4 строки добавить).

Проблема:
  «Топ модели» и «Доступные модели» показывают 0 req у всех моделей.
  В истории ключа реальные запросы видны.

РЕАЛЬНЫЙ корень (установлен при чтении кода):
  В freeapi/blueprints/chat_bp.py: update_model_stats() УЖЕ вызывается
  после каждого finish_request (строки 80, 99, 279, 306, 321).
  НО в freeapi/agent.py (AI-модератор): finish_request вызывается
  (строки 203, 210), а update_model_stats — НЕТ вообще.
  Значит, запросы через AI-агента (модерация отзывов) не учитываются в статистике.

  Вторая возможная причина: model_stats заполняется ТОЛЬКО для текущего месяца.
  При пустой БД или на старте месяца — строки вставляются через _seed_reference_data
  (INSERT OR IGNORE), но total_requests = 0. Запросы из chat_bp должны накапливаться.

  Дополнительная проверка: в _seed_reference_data() — row для каждой модели
  создаётся INSERT OR IGNORE, т.е. при уже существующей строке ничего не вставится.
  Если запросы уже делались, статистика должна быть ненулевой. Если нет — баг в agent.py.

Решение — добавить вызовы update_model_stats в agent.py:

  В freeapi/agent.py, функция _moderate(), блок try/except (≈строки 198-212):

  # Успешный ответ (строка ≈203):
  repo.finish_request(req_record['id'], 'ok', 'REQ_OK_001',
                      response_ms=int((_time.time() - _t0) * 1000))
  # ДОБАВИТЬ:
  try:
      from freeapi.repos.stats import update_model_stats
      update_model_stats(model, int((_time.time() - _t0) * 1000), ok=True)
  except Exception:
      pass

  # Ошибка (строка ≈210):
  repo.finish_request(req_record['id'], 'error', 'REQ_ERR_500',
                      response_ms=int((_time.time() - _t0) * 1000), ...)
  # ДОБАВИТЬ:
  try:
      from freeapi.repos.stats import update_model_stats
      update_model_stats(model, int((_time.time() - _t0) * 1000), ok=False)
  except Exception:
      pass

  Внимание: elapsed уже вычислен внутри блоков. Использовать сохранённое значение,
  не пересчитывать _time.time()-_t0 повторно (уже поздно). Лучше сохранить elapsed
  в переменную перед вызовом finish_request, как это сделано в chat_bp.py.

Push: «fix(stats): A2 — update_model_stats добавлен в agent.py».

────────────────────────────────────────
A3 — Цветовая индикация response_ms в истории ключа
────────────────────────────────────────
Статус: [✓] ВЫПОЛНЕНО
Сложность: минимальная (1 JS-файл, 5 строк).

Проблема:
  В истории запросов (views/keys.js, функция showHistory, строка 305):
    '<div class="hist-ms">' + (r.response_ms != null ? r.response_ms + ' мс' : '—') + '</div>'
  Нет цветовой индикации — пользователь не видит, быстро или медленно.

Реальная задержка Gemini: 9–12 секунд — это норма для thinking-моделей.
Цвета помогают понять контекст.

Решение — в static/js/views/keys.js, строка 305, заменить на:
  var _ms = r.response_ms;
  var _msColor = _ms == null ? '#555' : (_ms < 4000 ? '#22c55e' : (_ms < 8000 ? '#eab308' : '#ef4444'));
  '<div class="hist-ms" style="color:' + _msColor + '">' +
    (_ms != null ? _ms + ' мс' : '—') +
  '</div>'

  Пороги:
    < 4000 мс  → зелёный #22c55e
    4000–8000  → жёлтый  #eab308
    > 8000 мс  → красный #ef4444

Push: «fix(ui): A3 — цветная индикация response_ms в истории».

────────────────────────────────────────
A4 — Адаптив (мобильная вёрстка)
────────────────────────────────────────
Статус: [✓] ВЫПОЛНЕНО
Сложность: средняя (CSS + проверка sidebar.js).

Проблемы (по скриншотам):
  a) .bg-wrap (анимированный фон) не растягивается на всю высоту при
     малом контенте. Найти .bg-wrap в main.css → добавить min-height.
  b) Иконки-кнопки (.btn-icon) на мобиле сжимаются — нужен min-width.
  c) Боковое меню: overlay (#sidebarOverlay) уже реализован в sidebar.js
     и index.html. Проверить, работает ли tap-закрытие на мобиле.
     В sidebar.js closeSidebar() уже есть. Нужна проверка, что
     onclick на overlay вызывает closeSidebar() — скорее всего есть.
  d) Таблица моделей (#modelsTable или .docs-table) на узком экране
     переносит текст — сделать горизонтальный скролл.
  e) Карточки model-card: уменьшить padding на мобиле.

Решение — в static/css/main.css добавить:

  /* A4-a: фон на всю высоту */
  .bg-wrap { min-height: 100vh; min-height: 100dvh }

  /* A4-b: кнопки-иконки не сжимаются */
  .btn-icon { flex-shrink: 0 }
  @media(max-width:640px) {
    .btn-icon { min-width: 28px; min-height: 28px }
  }

  /* A4-d: таблица моделей с горизонтальным скроллом */
  @media(max-width:640px) {
    .docs-table-wrap { display:block; overflow-x:auto; -webkit-overflow-scrolling:touch }
    .docs-table { min-width:480px }
  }

  /* A4-e: model-card padding */
  @media(max-width:640px) {
    .model-card { padding: 14px }
  }

Перед записью — проверить реальные имена классов в index.html:
  grep -n "docs-table\|model-card\|bg-wrap" static/index.html static/css/main.css

Push: «fix(ui): A4 — мобильная адаптация».

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
БЛОК B — НОВЫЕ ФИЧИ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

────────────────────────────────────────
B1 — Smart Drafts: баннер о найденном черновике
────────────────────────────────────────
Статус: [✓] ВЫПОЛНЕНО
Сложность: малая (только index.html inline JS + немного CSS).

ВАЖНО: Автосохранение черновика УЖЕ РАБОТАЕТ:
  - oninput → lsSet('freeapi_review_draft', value) [index.html строка 1268]
  - После успешной отправки → lsDel(...) [строки 3136, 3590]
  - При открытии формы → lsGet('freeapi_review_draft', '') [строка 3414]
    НО: lsGet возвращает значение, но нет UI-баннера «Найден черновик».
  - reviews.js = заглушка (export {}), вся логика отзывов — в index.html inline.

Что нужно добавить (только в static/index.html):
  В функции, которая рендерит форму отзыва (вероятно _rvRenderWriteBar или
  при открытии секции writeReview):
    var _uid = window.user ? window.user.id : '';
    var _dk = 'freeapi_review_draft';
    var _draft = window.lsGet ? window.lsGet(_dk, '') : '';
    if (_draft && _draft.length >= 10 && !window._rvDraftBannerShown) {
      window._rvDraftBannerShown = true;
      // Вставить баннер выше textarea:
      // «Найден черновик [Восстановить] [Удалить черновик]»
      // При «Восстановить» → заполнить #writeReviewText значением _draft
      // При «Удалить» → lsDel(_dk), убрать баннер
    }

Дизайн баннера:
  Монохромный контейнер .rv-draft-banner (добавить CSS):
    background:#0a0a0a; border:1px solid #1e1e1e; border-radius:8px;
    padding:10px 14px; font-size:12px; color:#777; display:flex;
    align-items:center; justify-content:space-between; margin-bottom:10px
  Без эмодзи, без SVG-украшений — только текст + 2 кнопки.

Также добавить в lsDel при удалении черновика нужный ключ в onDelete-callback.

Push: «feat(reviews): B1 — баннер черновика (Smart Drafts)».

────────────────────────────────────────
B2 — Группировка уведомлений по типам (kind)
────────────────────────────────────────
Статус: [✓] ВЫПОЛНЕНО
Сложность: средняя. Бэкенд + фронт + миграция.

Шаг B2.1 — миграция (новый файл freeapi/migrations/007_notif_kind.sql):
  ALTER TABLE user_notifications ADD COLUMN kind TEXT NOT NULL DEFAULT 'system';
  ALTER TABLE user_notifications ADD COLUMN ref_id TEXT;
  CREATE INDEX IF NOT EXISTS idx_user_notif_user_kind
    ON user_notifications(user_id, kind);

Типы kind:
  'review'  — решение по отзыву (APPROVE/DELETE/FEEDBACK/ответ владельца)
  'support' — ответ в поддержке (пока резервный, поддержка пишет напрямую в чат)
  'system'  — всё остальное

Шаг B2.2 — бэкенд (freeapi/repos/notifications.py):
  Изменить сигнатуры:
    create_user_notification(user_id, message, kind='system', ref_id=None)
      → INSERT ... kind, ref_id (новые колонки)
    get_user_notifications(user_id, kind=None, limit=50, offset=0)
      → если kind → WHERE kind=?
    count_unread_notifications(user_id, kind=None)
      → если kind → WHERE kind=?
    mark_all_notifications_read(user_id, kind=None)
      → если kind → WHERE kind=?

Шаг B2.3 — проставить kind при вызовах create_user_notification:
  freeapi/agent.py:
    APPROVE (строка 228):  kind='review', ref_id=review_id
    FEEDBACK (строка 250): kind='review', ref_id=review_id
    DELETE (строка 261):   kind='review', ref_id=review_id
  freeapi/blueprints/reviews_bp.py:
    Ответ владельца (строка 233): kind='review', ref_id=review_id

Шаг B2.4 — API (freeapi/blueprints/notifications_bp.py):
  GET /api/notifications?kind=&limit=&offset=
  → ответ: {
      notifications: [...],
      unread: { all: N, review: N, support: N, system: N }
    }
  POST /api/notifications/read_all?kind=
  → помечает только нужный kind (или все, если kind не задан)

Шаг B2.5 — фронт (static/js/views/notifications.js):
  Табы: «Все» / «Отзывы» / «Система»
  (support-таб добавить как резерв, но скрыть если count=0)
  Иконки табов — inline SVG 14×14, stroke=currentColor:
    Все     — колокольчик
    Отзывы  — звезда
    Система — шестерёнка
  Активный таб: белая обводка, неактивный: серый.
  Бейдж непрочитанных у каждого таба.
  setNotifFilter(kind) → GET /api/notifications?kind=..., обновляет список.
  refreshNotifBadge: возвращает {all,review,support,system} — бейдж на колокольчик = all.
  Дип-линк по клику на карточку (data-kind + data-ref-id):
    review + ref_id → goView('reviews')
    system          → остаться

Шаг B2.6 — CSS (static/css/main.css):
  .notif-tabs { display:flex; gap:4px; margin-bottom:16px;
                border-bottom:1px solid #141414; padding-bottom:12px }
  .notif-tab { display:inline-flex; align-items:center; gap:6px;
               padding:6px 14px; border-radius:8px; font-size:12px;
               color:#555; cursor:pointer; transition:all .15s; border:1px solid transparent }
  .notif-tab.active { color:#fff; background:rgba(255,255,255,.06); border-color:#222 }
  .notif-tab-badge { min-width:16px; height:16px; padding:0 4px; border-radius:8px;
                     background:#3a1010; color:#f87171; font-size:10px; font-weight:700;
                     line-height:16px; text-align:center; display:inline-block }

Push: два коммита:
  1. «feat(notif): B2 бэкенд — миграция 007 + API kind/ref_id»
  2. «feat(notif): B2 фронт — табы по типам уведомлений»

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
БЛОК C — УСТАРЕВШИЕ ДАННЫЕ (документация)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

────────────────────────────────────────
C1 — support_docs.py: неточности в реестре документации
────────────────────────────────────────
Статус: [✓] ВЫПОЛНЕНО
Сложность: минимальная (1 Python-файл, правка текста).

Найденные ошибки в freeapi/support_docs.py:

  1) Раздел 'reviews' (строка 119):
       «Поля отзыва: оценка 1-5 звёзд, текст...»
       → РЕАЛЬНО: оценка 1-10, без звёзд (числовые кнопки).
       Исправить: «Поля отзыва: оценка от 1 до 10, текст...»

  2) Раздел 'api_keys' (строка 54):
       «Можно восстановить из pending_restore»
       → РЕАЛЬНО: pending_restore — это структура для восстановления
         ПАМЯТИ (context_md/favorite_md) после /reset, НЕ ключей.
         Удалённый ключ (is_active=0) не восстанавливается через pending_restore.
       Исправить: убрать эту фразу или уточнить «мягкое удаление без восстановления».

  3) Раздел 'notifications' (строка 188):
       «Опрос /api/notifications каждые 60 секунд» — актуально.
       «Источники уведомлений: ответ админа на твой отзыв, алерты по ключам»
       → На самом деле источники: agent APPROVE/DELETE/FEEDBACK, ответ владельца вручную.
         Никаких «алертов по ключам» пока нет. Уточнить.

Push: «fix(docs): C1 — исправлены неточности в support_docs.py».

────────────────────────────────────────
C2 — README.md: устаревшая структура
────────────────────────────────────────
Статус: [✓] ВЫПОЛНЕНО
Сложность: малая (только документация).

Найденные устаревания в README.md:

  1) Структура проекта: показан старый repositories.py как монолит.
     Реально: freeapi/repos/ (keys.py, stats.py, notifications.py, reviews.py,
     support.py, tg_accounts.py, users.py, admin.py) + blueprints/.

  2) API Endpoints: отсутствуют:
     GET/POST /api/reviews, /api/reviews/mine, /api/reviews/<id>/like
     GET/POST /api/notifications, /api/notifications/read_all, /api/notifications/<id>
     GET/POST /api/support/chat, /api/support/chat/stream, /api/support/close
     GET/PUT /api/admin/settings, /api/admin/notifications, ...
     GET /api/models с totalRequests/supportsVision

  3) Возможности: не упомянуты:
     AI-модерация отзывов (FavoriteAIAgent)
     Поддержка (Favorite AI Agent поддержки с doc-тегами)
     Система уведомлений
     Dual-mode (два TG-аккаунта)
     Smart Drafts, лайки отзывов, avg_score
     Система памяти (agent_memory: context_md, favorite_md)
     Ring-buffer логов + дамп в logi.txt

  4) Зависимости: Flask указан как 3.0.3, Python 3.8+ →
     Реально Python 3.11+ (используется современный синтаксис).

Push: «docs: C2 — актуализирован README.md».

──────────────────────────────────────────────────────────
ПОРЯДОК ВЫПОЛНЕНИЯ
──────────────────────────────────────────────────────────

Рекомендуемый порядок:
  1. A0 — 5 мин. Уберём эмодзи — соответствие правилам.
  2. A1 — 5 мин. Быстрая CSS-правка.
  3. A2 — 15 мин. Важно, данные отображаются некорректно.
  4. A3 — 5 мин. Быстрая UI-покраска.
  5. A4 — 30 мин. Адаптив, несколько мест в CSS.
  6. B1 — 20 мин. Баннер черновика, только inline index.html.
  7. B2 — 60 мин. Миграция + бэкенд + фронт (самая крупная задача).
  8. C1 — 10 мин. Документация support_docs.py.
  9. C2 — 20 мин. README.md.

После каждого пункта — коммит + пуш через GitHub API.

──────────────────────────────────────────────────────────
ТЕХНИЧЕСКИЕ ПРАВИЛА (не менять между сессиями)
──────────────────────────────────────────────────────────

  • Эмодзи на сайте ЗАПРЕЩЕНЫ — только inline SVG (stroke=currentColor).
  • Это касается в том числе строк уведомлений в Python-коде.
  • Один шаг = один коммит = один пуш через GitHub API.
  • После каждого пуша обновить этот plan.txt (статус → ✓ ВЫПОЛНЕНО).
  • Git push только через GitHub API (JS fetch в code_execution).
  • Работаем только с существующим кодом — никакого Node.js/React.
  • views/reviews.js — пока заглушка. B1 делается в index.html (inline JS).
  • Перед любым изменением CSS/JS — прочитать реальный код файла,
    не полагаться на memory: имена классов могут отличаться от плана.

══════════════════════════════════════════════════════════
  D2 — UI-правки по фидбеку владельца (✓ ВЫПОЛНЕНО)
  ══════════════════════════════════════════════════════════

    1. .sb-role — text-align:center, чтобы бейдж «ВЛАДЕЛЕЦ» в сайдбаре
       не прижимался к левому краю.
    2. .badge-moderator — заменили статичный синий стиль на медленный
       плавный градиент (8s ease-in-out, палитра #3b82f6 → #7eb6ff →
       #e0eaff → #7eb6ff → #3b82f6) по аналогии с .badge-owner.
    3. .notif-tab-cnt — серый плашечный счётчик (bg #1f1f1f, активный
       #2f2f2f), фиксированная коробка 18×18, цифра идеально по центру.
    4. total_by_kind — добавили repo.count_notifications_by_kind +
       поле total_by_kind в GET /api/notifications. На фронте
       _renderNotifTabsCounts использует total_by_kind, чтобы цифра
       на табе оставалась после прочтения уведомления (раньше уходила
       в 0 и выглядела как баг).
    5. .dash-section-title — margin-top:18px (заголовок «API Ключи» /
       «Новый ключ» больше не прижимается к шапке).
    6. response_ms пороги в keys.js: <8000 зелёный, <15000 жёлтый,
       >=15000 красный. TG-мост через @SamGPTrobot отвечает 8–12с —
       это норма, поэтому 4с-порог давал ложно-жёлтые значения.
    7. .hist-back — margin-top:14px (кнопка «Назад» в истории больше
       не прилипает к шапке).

  Push: «fix(ui): D2 — централизация ВЛАДЕЛЕЦ, градиент МОДЕРАТОР,
  серые счётчики табов, total_by_kind, отступы заголовков, пороги
  response_ms».

  ══════════════════════════════════════════════════════════
  D3 — System Prompt в документации (✓ ВЫПОЛНЕНО)
  ══════════════════════════════════════════════════════════

  Что было не так в старом промпте (#memorySystemPrompt в index.html):

    1. Список моделей врал. Указывались gemini-2.5-pro и
       gemini-2.0-flash, которых в freeapi/models.py НЕТ. Фактически
       лучшие модели у нас — gemini-3.0-flash-thinking (DEFAULT) и
       gemini-3.0-flash. Полный список (см. AI_MODELS):
         • 200k: 3.0-flash, 3.0-flash-thinking, 2.5-flash,
                 2.5-flash-thinking, 2.5-mini, 2.5-mini-thinking,
                 1.5-robotics-er-preview
         • 64k:  3.0-flash, 3.0-flash-thinking, 2.5-flash,
                 2.5-flash-thinking
    2. Не было упомянуто, что поле «model» опционально — если не
       передать, используется модель ключа по умолчанию (выбирается
       в дашборде).
    3. Не было сказано, что нормальное время ответа TG-моста — 8–12с,
       и это не таймаут (это коррелирует с D2-6).
    4. Пример в Body указывал «gemini-2.5-flash» вместо актуальной
       рекомендованной модели «gemini-3.0-flash-thinking».
    5. Не упомянуто, что живой список моделей доступен по
       GET /api/models — внешний ИИ может его подтянуть сам.

  Все пункты починены в новом промпте. Текст хранится в одном месте —
  static/index.html, тег <pre id="memorySystemPrompt">.

  Push: «docs: D3 — actualize system prompt (real model ids, default
  model, response time note, /api/models hint)».

  ══════════════════════════════════════════════════════════
  E1 — GET /api/v1/models под Bearer-ключом (✓ ВЫПОЛНЕНО)
  ══════════════════════════════════════════════════════════

  Зачем: после D3 в system prompt появилась подсказка про живой
  список моделей через GET /api/models. Но публичный /api/models
  отдаёт сырой ответ со статистикой запросов (totalRequests/
  avgResponseMs) и ничего не знает про конкретный ключ.
  Внешнему ИИ полезно знать ровно три вещи:
    • какие id моделей вообще существуют (чтобы не слать
      несуществующие, типа gemini-2.5-pro);
    • какая модель будет использована, если поле "model" опущено
      (= default ключа, см. D3);
    • какие модели мы рекомендуем (3.0-flash-thinking, 3.0-flash).

  Реализация (один файл — freeapi/blueprints/misc_bp.py):
    • Константа RECOMMENDED_MODEL_IDS = ('gemini-3.0-flash-thinking',
      'gemini-3.0-flash').
    • GET /api/v1/models — авторизация через authorized_key()
      (Bearer <api_key>), как у /api/v1/chat и /api/v1/reset.
    • Ответ:
        {
          models: [{
            id, displayName, contextK, supportsVision,
            isDefault, isPopular, isRecommended
          }, ...],
          defaultModelId: 'gemini-3.0-flash-thinking',
          keyDefaultModelId: <default_model ключа или DEFAULT_MODEL_ID>,
          recommended: ['gemini-3.0-flash-thinking', 'gemini-3.0-flash']
        }
    • Сознательно НЕТ полей totalRequests/avgResponseMs/tgCallback —
      это «потребительский» вид, ничего лишнего наружу.

  Документация (static/index.html):
    • В таблицу Bearer-эндпоинтов добавлена строка GET /api/v1/models.
    • Старая строка про /api/models помечена как «публичный, со
      статистикой» — чтобы не было путаницы с v1.
    • Поле «model» в таблице Body POST /api/v1/chat переведено в
      «Обязательно: Нет» с уточнением про default ключа (косяк из D3
      — в промпте было сказано, что model опционален, а в таблице
      по-прежнему стояло «Да»).
    • Блок «Available Models» в #memorySystemPrompt дополнен:
      теперь упомянуты ОБА эндпоинта (public /api/models и Bearer
      /api/v1/models), внешний ИИ предупреждён, что v1 удобнее, т.к.
      сразу отдаёт keyDefaultModelId.

  Push: «feat(api): E1 — GET /api/v1/models под Bearer + обновлён
  system prompt и таблица документации».

  ══════════════════════════════════════════════════════════
  E2 — GET /api/v1/me (контекст ключа за один запрос) (✓ ВЫПОЛНЕНО)
  ══════════════════════════════════════════════════════════

  Зачем: даже после E1 у внешнего ИИ нет дешёвого способа узнать,
  «а кто я вообще, какой у меня ключ, не висит ли он, насколько
  забит контекст». Раньше пришлось бы парсить ошибки CTX_LIMIT_180
  и KEY_BUSY_301 постфактум. Теперь — один GET /api/v1/me на старте
  сессии.

  Реализация (freeapi/blueprints/misc_bp.py):
    • Авторизация — authorized_key() (Bearer <api_key>).
    • Используется repo.get_user_by_id и repo.get_key_month_stats
      (оба уже есть, экспортируются через repos/__init__.py).
    • Маска ключа — security.mask_key (импорт внутри функции, чтобы
      не плодить module-level зависимости).
    • Никогда не возвращаются: чистый key_value, user_id, key_id,
      tg_account_id, password_hash. Это «потребительский» вид.

  Структура ответа:
    {
      key: {
        name, masked, default_model, dual_mode, context_kb,
        context_warn_kb, context_limit_kb, context_warn,
        limit_hit, is_busy, created_at
      },
      owner: { username, is_admin (=username=='ReZero') },
      stats: { monthly_requests, avg_response_ms },
      service: {
        default_model_id, recommended, models_endpoint
      }
    }

  Документация:
    • static/index.html — таблица Bearer-эндпоинтов: добавлена строка
      GET /api/v1/me с описанием.
    • #memorySystemPrompt: блок NOTE про эндпоинты переписан.
      Теперь упомянуты все три (/api/models, /api/v1/models,
      /api/v1/me) и явно сказано: «recommended bootstrap — вызвать
      /api/v1/me один раз в начале сессии». Это закрывает кейсы,
      когда внешний ИИ не знает, какая модель ключа default,
      сколько KB уже накопилось и не занят ли ключ.

  Push: «feat(api): E2 — GET /api/v1/me (контекст ключа: владелец,
  default model, context_kb, dual_mode, лимиты) + документация».

  ══════════════════════════════════════════════════════════
  F1 — Аудит «фантомного» эндпоинта /api/v1/chat/completions (✓ ВЫПОЛНЕНО)
  ══════════════════════════════════════════════════════════

  Найдено три связанных бага по grep'у `completions`:

  1. freeapi/app.py — _RATE_LIMIT_RULES:
       (('POST',), ('/api/chat/test', '/api/v1/chat/completions'), 30, 60)
     Реальный эндпоинт чата — /api/v1/chat (chat_bp.py:183).
     Эндпоинта /api/v1/chat/completions в коде НЕТ. Значит rate-limit
     30/60 на чат фактически НЕ применялся — полностью открытый
     эндпоинт. Исправлено: путь заменён на /api/v1/chat.

  2. freeapi/blueprints/admin_bp.py (системный промпт админ-агента):
     §4.1 описывал «POST /api/v1/chat/completions, ПОЛНОСТЬЮ совместим
     с OpenAI Chat Completions API, можно подсунуть любой OpenAI SDK».
     Это неправда: путь другой, ответ упрощённый (нет usage / object /
     finish_reason / index), поле "model" опционально (берётся default
     ключа). Перепиcан §4.1 с честным описанием формата ответа и
     честной оговоркой про OpenAI SDK. Пример с openai-клиентом
     заменён на requests. URL в curl-примере исправлен.
     Также добавлены §4.9 GET /api/v1/models и §4.10 GET /api/v1/me
     (актуализация под E1+E2).

  3. freeapi/support_docs.py:
     В блоках 'api_keys' и 'chat' были ссылки «по адресу
     /v1/chat/completions» / «под капотом тот же /v1/chat/completions».
     Заменено на POST /api/v1/chat.

  Итог: rate-limit на главный эндпоинт восстановлен, документация
  для админ-агента и агента поддержки больше не врёт пользователям
  про несуществующий URL и про OpenAI SDK.

  ══════════════════════════════════════════════════════════
  F2 — Аудит §4 документации админ-агента (✓ ВЫПОЛНЕНО)
  ══════════════════════════════════════════════════════════

  Сравнил все @bp.* роуты с тем, что обещает админ-документация
  в admin_bp.py § 4. Найдено два критичных расхождения:

  1. §4.4 POST /api/keys — документация описывала это как «создать
     новый ключ, body {name}, ответ 201 с key_value». На самом деле
     keys_bp.py:74 ВСЕГДА возвращает 409 с подсказкой "Запустите
     SetupFlow через /api/tg/setup". То есть админ-агент мог бы
     рекомендовать пользователю несуществующий способ создания
     ключа. Перепиcан §4.4 с честной формулировкой; добавлен новый
     §4.12 «Создание API-ключа (SetupFlow)» с реальной
     последовательностью /api/tg/setup → /code → /status.

  2. §4.2 GET /api/models — документация указывала поле
     "hasThinking", которого в реальном ответе НЕТ. Реальные поля
     (misc_bp.py:72): id, displayName, contextK, supportsVision,
     isDefault, isPopular, avgResponseMs, totalRequests. Исправлено;
     добавлено явное указание что hasThinking/isRecommended есть
     только в /api/v1/models.

  Также добавлен §4.11 — ранее в админ-доке вообще не были упомянуты
  Bearer-эндпоинты управления контекстом: POST /api/v1/stop,
  POST /api/v1/reset, POST /api/v1/reset/apply. Это критично для
  внешних клиентов, которые иначе не знают, что делать после
  CTX_LIMIT_180 или KEY_BUSY_301.

  ══════════════════════════════════════════════════════════
  F3 — Аудит публичной документации (README + index.html) (✓ ВЫПОЛНЕНО)
  ══════════════════════════════════════════════════════════

  Распространил находки F1/F2 на публичную документацию:

  1. static/index.html — раздел «Python (OpenAI SDK)» содержал
     полностью нерабочий пример: `client.chat.completions.create()`
     ходит в /chat/completions, которого у нас нет, и ждёт полей
     usage / object / finish_reason / index, которых тоже нет.
     Заменено на честный блок «Совместимость с OpenAI SDK» с
     объяснением почему стандартный SDK не работает и отсылкой
     к примерам с requests / fetch / curl выше.

  2. README.md:
     • Секция «Чат / API v1» расширена до «Чат / API v1 (Bearer)»
       с полным актуальным списком: /api/v1/chat (с пометкой что
       model опциональна), /api/v1/stop, /api/v1/reset,
       /api/v1/reset/apply (новое), /api/v1/me (E2),
       /api/v1/models (E1).
     • Добавлена секция «Внутренний чат (cookie-сессия, для веб-UI)»
       с /api/chat/test, /api/chat/reset, /api/chat/reset/apply —
       раньше эти эндпоинты вообще не были задокументированы.
     • POST /api/keys: переписано на честное «Всегда 409: ключ
       создаётся как побочный эффект /api/tg/setup, прямого
       создания нет».
     • Уточнены поля PUT /api/keys/:id (defaultModel, dualMode,
       translatorAccountId).
     • Telegram-мост: добавлен POST /api/tg/session/import (раньше
       не был задокументирован).

══════════════════════════════════════════════════════════
F4 (3f21392) — Аудит UI-вьюх на фантомные эндпоинты
══════════════════════════════════════════════════════════
  Сверил все вызовы /api/* из static/ со списком всех @bp-роутов
  (включая @bp.route()) в freeapi/blueprints/.

  Найден один реальный фантом:
  • static/js/views/setup.js — функция downloadSession()
    зовёт GET /api/tg/account/session, которого в backend НЕТ.
    К UI она НЕ привязана (нет ни одного onclick=...) — мёртвый
    код, оставшийся от рефакторинга. Рабочее скачивание .session
    есть на дашборде ключей: downloadKeySession() в keys.js
    через GET /api/keys/<id>/session.
    Удалена функция и её export window.downloadSession.

  Все остальные UI-вызовы соответствуют существующим роутам:
    /api/auth/{login,logout,me,register}, /api/_clog,
    /api/healthz, /api/log-codes, /api/models,
    /api/keys[/:id[/{logs,regen,session}]],
    /api/notifications[/{:id[/read],read_all}],
    /api/admin/{notifications[/:id],settings,support/chat/:id},
    /api/reviews[/{mine,:id[/{like,status}]}],
    /api/stats/{global,keys/:id},
    /api/support/{chat[/stream],close},
    /api/chat/{reset[/apply],test},
    /api/tg/{session/import,setup[/:id/{cancel,code,retry,status}],account},
    /api/v1/{chat,me,models,reset[/apply],stop}.

  Backend-роут без потребителя в UI: GET /api/admin/reviews
  (UI намеренно удалил вызов в 0.5.13). Эндпоинт безопасен,
  возвращает админу пагинированный список отзывов — оставлен
  как есть (может вызываться руками/админ-агентом).

══════════════════════════════════════════════════════════
══════════════════════════════════════════════════════════

══════════════════════════════════════════════════════════
F5 — Аудит безопасности роутов (✓ ВЫПОЛНЕНО, находок нет)
══════════════════════════════════════════════════════════
  Прошёл по всем blueprints, нашёл 3 маршрута без явного
  require_user/require_admin/authorized_key. Все три в порядке:
    • /api/auth/me — по дизайну отвечает «не залогинен» вместо 401.
    • /api/log-codes — публичный справочник кодов журнала.
    • /api/admin/support/chat/<id> — проверка прав встроена
      вручную (session.uid + user.is_admin), без helper-функции.
  Незащищённых админских/пользовательских маршрутов нет.

══════════════════════════════════════════════════════════
F6 — Целостность реестра кодов журнала (✓ ВЫПОЛНЕНО)
══════════════════════════════════════════════════════════
  Сверил ВСЕ коды, реально записываемые backend-ом
  (в repo.finish_request, error(...), raise RuntimeError(...),
  jsonify({'log_code': ...}) и пр.), со справочником
  freeapi/log_codes.py (он же /api/log-codes).

  НАЙДЕНО 6 кодов, которые сервер реально пишет, но в
  справочнике отсутствовали — пользователь видел бы их в
  логах «без расшифровки»:

    • CTX_LIMIT_180  — переполнение контекста Telegram-бота
                       (raise в tg.py, error 413 в chat_bp.py).
    • REQ_ERR_500    — внутренняя ошибка обработки запроса
                       (finish_request в agent.py + chat_bp.py).
    • RATE_LIMIT_429 — IP-rate-limit в app.py (jsonify 429).
    • DUAL_OK_801    — успех в dual_mode (chat_bp.py, /chat/test
                       и /v1/chat).
    • MEM_WRITE_901  — записан context_md / favorite_md
                       (chat_bp.py /v1/chat).
    • MEM_RESTORE_903 — pending_restore после reset
                       (chat_bp.py /chat/reset + /v1/reset).

  Все 6 добавлены в RAW_LOG_CODES с описанием и (где уместно)
  с подсказкой решения. Категории:
    Контекст | Запросы | Лимиты | Двойной режим | Память.

  Обратная сверка (declared but never emitted): 23 кода —
  это запланированный задел под будущие шаги мастера TG,
  спонсоров, промокодов и подсказок бота. Они задокументированы
  заранее и не мешают — оставлены как есть.


╔══════════════════════════════════════════════════════════╗
║  G5 — АВАТАРКА ПРОФИЛЯ (загрузка + кадрирование)         ║
║  Статус: РЕАЛИЗОВАНО.                                     ║
╚══════════════════════════════════════════════════════════╝

  Цель: дать пользователю возможность загрузить и обрезать
  свою аватарку прямо через сайдбар, без сторонних JS-библиотек
  (Termux-friendly).

  Архитектура хранения:
    • Поле users.avatar TEXT (NULL допустим).
      Хранится готовый data URL (jpeg/png base64).
    • Никаких файловых блобов на диске — простой бэкап через
      sqlite3 .backup, не плодим вторую систему файлов.
    • Лимит: ≤ 200 KB после кадра, длина строки ≤ 280 000 симв.

  Миграция: 008_user_avatar.sql — ALTER TABLE users ADD COLUMN.

  Бэкенд (freeapi/blueprints/auth_bp.py):
    • PUT    /api/auth/avatar
        body: {"data_url": "data:image/jpeg;base64,..."}
        валидация: префикс jpeg/png, длина, decoded size ≤ 200 KB.
        log: AVA_SET_111 — Категория «Профиль».
    • DELETE /api/auth/avatar
        сбрасывает поле в NULL.
        log: AVA_DEL_112.
    • GET    /api/auth/me — теперь подмешивает avatar в user
        (через repo.get_user_avatar, отдельным запросом).

  Репозиторий (freeapi/repos/users.py):
    • get_user_avatar / set_user_avatar / clear_user_avatar.
    • Намеренно НЕ добавляли avatar в get_user_by_id —
      чтобы все существующие места не таскали блоб.

  Коды журнала (log_codes.py):
    AVA_SET_111 | Профиль | Аватарка профиля обновлена |
    AVA_DEL_112 | Профиль | Аватарка профиля удалена   |

  Фронтенд:
    • Новый ESM-модуль static/js/views/profile_avatar.js
      (зарегистрирован в static/js/main.js).
      ─ Делегированные обработчики click/change.
      ─ Cropper в <canvas 320×320>: drag (mouse + 1 touch),
        zoom через колесо/2-touch-pinch/range-input.
      ─ Экспорт в финальный canvas 256×256 → toBlob(jpeg)
        с понижением качества 0.92 → 0.85 → 0.78, чтобы
        результат стабильно влезал в 180–200 KB.
      ─ После успеха — window.user.avatar = ...; renderSidebarAvatar.
    • Новая модалка #avatarCropModal (static/index.html).
      Скрытый <input type="file" #sbAvatarFile> — accept
      jpeg/png/webp/gif, чтоб телефонная галерея «увидела» файл.
    • .sb-avatar получает data-auth="1" для авторизованных,
      role="button", tabindex="0", title="Сменить аватарку".
      hover/focus подсветка через CSS.
    • renderSidebarAvatar(user|null):
        – нет user → svg-плейсхолдер,
        – avatar есть → <img class="sb-avatar-img">,
        – иначе → первая буква username.
    • Inline-блок updateAuth() переключён на renderSidebarAvatar
      (с fallback-веткой на старое поведение, если модуль ещё
      не загрузился — defer-порядок).

  Стили (static/css/main.css):
    • .sb-avatar overflow:hidden + .sb-avatar-img object-fit:cover.
    • .sb-avatar[data-auth="1"]: cursor + hover transform/border.
    • .avatar-crop-stage / #avatarCropCanvas / .avatar-crop-zoom.
    • Адаптация ≤ 420 px (canvas 280×280).


╔══════════════════════════════════════════════════════════╗
║  G6 — ФОН-СЕТКА НА ШИРОКИХ ЭКРАНАХ                       ║
║  Статус: РЕАЛИЗОВАНО.                                     ║
╚══════════════════════════════════════════════════════════╝

  Симптом: на десктопе/широком окне по бокам сцены появлялись
  чёрные «пустоты» — фон-сетка не дотягивалась до краёв.

  Корень проблемы:
    .bg-mesh имела размер 160% × 160% (top:-30%, left:-30%),
    маска — radial-gradient(ellipse at 50% 50%, black 30%, transparent 82%).
    Эллипс на 16:9 viewport-е получался коротким по горизонтали:
    radial-gradient «ellipse» по умолчанию использует
    farthest-corner с пропорциями контейнера, и при ширине ≫ высоты
    стопы 30/82 % приходились на относительно узкую среднюю полосу.

  Фикс (main.css):
    1. Заменили маску на круглую с явным размером:
         radial-gradient(circle farthest-side at 50% 50%,
                         #000 60%, transparent 100%)
       Теперь активная зона — круг, привязанный к МЕНЬШЕМУ
       измерению контейнера (а сам контейнер — viewport+overscroll),
       но за счёт увеличения сетки на широких — круг гарантированно
       перекрывает экран по бокам.
    2. Медиа-точки:
         @media(min-width:1100px) → bg-wrap inset:-300px,
                                    bg-mesh 200%/200%, top/left -50%.
         @media(min-width:1700px) → bg-wrap inset:-400px,
                                    bg-mesh 240%/240%, top/left -70%.

  Не трогалось: правило `body:has(#view-dashboard.active)` и
  скрытие .bg-wrap на дашборде (там нужен полностью чёрный фон).


╔══════════════════════════════════════════════════════════╗
║  G7 — РАСПРОСТРАНЕНИЕ АВАТАРКИ (карточки + topbar)       ║
║  Статус: РЕАЛИЗОВАНО.                                     ║
╚══════════════════════════════════════════════════════════╝

  Цель: показывать аватарку пользователя везде, где встречается
  его имя — в карточках отзывов и в шапке (быстрый доступ к
  сайдбару).

  Бэкенд (freeapi/repos/reviews.py):
    Добавили u.avatar в три SELECT-а:
      • get_approved_reviews — публичный список (явные колонки).
      • get_pending_reviews  — для админ-модерации.
      • get_all_reviews_admin — полный список в админке.
    На /api/v1/reviews и админских ручках теперь приходит
    r.avatar (data URL или null) — без изменения формата ответа.

  Фронтенд:
    • static/js/views/profile_avatar.js — два новых хелпера в window:
        ─ renderAuthorChip(username, avatar, badgeHtml)
            Возвращает HTML «аватарка-кружок + имя + бейдж»
            для разметки .review-author. Если avatar нет —
            кружок с заглавной буквой ника.
        ─ renderTopbarAvatar(user|null)
            Рендерит #topbarAvatar: <img> или буква. При null
            кнопка скрывается (display:none).
    • static/index.html:
        ─ В .topbar — новая кнопка #topbarAvatar (32×32 кружок,
          onclick=toggleSidebar). Рядом с колокольчиком, перед
          гамбургером. Скрыта по умолчанию, показывается только
          для авторизованных.
        ─ Inline updateAuth() дёргает renderTopbarAvatar(user|null)
          в обеих ветках (вход/выход).
        ─ Оба места рендера .review-card (публичный и админский)
          переключены на renderAuthorChip с fallback на старую
          SVG-разметку, если модуль ещё не подгружен.

  Стили (static/css/main.css):
    • .topbar-avatar / .topbar-avatar-img — круглая 32×32 кнопка
      в шапке с hover-эффектом и focus-обводкой.
    • .author-avatar / .author-avatar-img / .author-avatar-letter —
      компактный 22×22 кружок для карточек отзывов (margin-right
      6 px, чтобы не ломать существующий горизонтальный layout
      .review-author).

  Что НЕ трогалось специально:
    • Уведомления (#notifPageList): они адресованы самому юзеру,
      «автор» там не отображается — добавлять аватар бессмысленно.
    • Ответ модератора (.review-answer / renderReplyAuthor):
      это всегда либо «Favorite AI Agent», либо «ReZero» —
      не пользовательские профили, аватарка не применима.

══════════════════════════════════════════════════════════
КОНЕЦ ПЛАНА
══════════════════════════════════════════════════════════
